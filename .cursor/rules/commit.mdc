---
description: How to commit changes — NEVER push unless user explicitly asks
alwaysApply: false
---

# Commit — Never Push Automatically

**NEVER run `git push` unless the user explicitly says "push" or "push the branch".**
After committing, stop. The user will push and open a PR themselves.

## 1. Regenerate OpenAPI spec

```bash
npx tsx scripts/generate-openapi.ts
```

If the spec changed, stage `docs/openapi.json` along with all other files.

## 2. Create branch (if on main)

If currently on `main`, create a new descriptively-named branch **before** committing:

```bash
git checkout -b <descriptive-branch-name>
```

## 3. Stage and commit

```bash
git add -A
git commit -m "..."
```

Use a conventional commit message summarizing the changes.

## 4. Stop here

Do **not** run `git push`. Do not open a PR. Just tell the user the commit is done and which branch it's on.
